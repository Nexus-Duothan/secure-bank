import React from 'react';
import { Typography } from 'antd';

const TransactionHistory: React.FC = () => {
  return <Typography.Title level={2}>Transaction History</Typography.Title>;
};

export default TransactionHistory;
