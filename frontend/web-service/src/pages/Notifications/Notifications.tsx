import React from 'react';
import { Typography } from 'antd';

const Notifications: React.FC = () => {
  return <Typography.Title level={2}>Notifications</Typography.Title>;
};

export default Notifications;
