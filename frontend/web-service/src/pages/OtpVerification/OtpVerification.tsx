import React from 'react';
import { Typography } from 'antd';

const OtpVerification: React.FC = () => {
  return <Typography.Title level={2}>Verify OTP</Typography.Title>;
};

export default OtpVerification;
