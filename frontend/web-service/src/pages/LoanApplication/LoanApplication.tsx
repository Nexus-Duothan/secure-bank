import React from 'react';
import { Typography } from 'antd';

const LoanApplication: React.FC = () => {
  return <Typography.Title level={2}>Loan Application</Typography.Title>;
};

export default LoanApplication;
