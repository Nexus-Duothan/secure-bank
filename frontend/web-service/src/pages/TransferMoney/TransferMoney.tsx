import React from 'react';
import { Typography } from 'antd';

const TransferMoney: React.FC = () => {
  return <Typography.Title level={2}>Transfer Money</Typography.Title>;
};

export default TransferMoney;
