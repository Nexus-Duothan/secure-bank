server:
  port: 8085

spring:
  application:
    name: transfer-service
  datasource:
    url: jdbc:postgresql://localhost:5432/securebank
    username: securebank
    password: securebank_secret_pass
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092
